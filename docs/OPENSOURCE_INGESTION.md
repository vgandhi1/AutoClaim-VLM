# Open-Source Ingestion

Implementation code lives under `src/opensourceingest/` and `src/glue/opensource_consolidation.py`.

The authoritative dataset strategy, normalization maps, and legal guardrails are documented in the repository root file `module_opensource_data_ingestion.md`.
