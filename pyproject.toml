[project]
name = "claimlens"
version = "0.1.0"
description = "VLM-powered vehicle damage assessment pipeline"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.coverage.run]
source = ["src"]
